404-0 = Could not find version information
404-1 = Could not start because there is no start command to run
404-2 = Could not stop because there is no stop command to run
404-3 = Could not serve log files because the configured log folder is invalid
404-4 = Could not serve install log files because the configured install log folder is invalid

400-0 = Error retrieving version information
400-1 = Could not serve log files. There is a configuration issue with the configured folder.
400-2 = Could not serve install log files. There is a configuration issue with the configured folder.